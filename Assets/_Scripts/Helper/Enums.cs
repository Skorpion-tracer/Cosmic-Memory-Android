namespace CosmicMemory.Helper
{
    public enum GameState : byte
    {
        None,
        Start,
        Pause,
        Game
    }

    public enum LevelHard : byte
    {
        Easy,
        Medium,
        Hard,
        Extreme
    }

    public enum TypeSwitchImage : byte
    {
        Free,
        Paid
    }
}
